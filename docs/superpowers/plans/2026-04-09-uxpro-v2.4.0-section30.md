# Section 30: Form Builder Patterns Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add comprehensive Section 30 (Form Builder Patterns) to UXPro design system, teaching AI tools to generate dynamic, adaptive forms with conditional visibility, multi-step wizards, field dependencies, and custom validation.

**Architecture:** Add Section 30 to UIUX-MASTER-PROMPT-SYSTEM.md (950-1100 lines covering basic dynamic forms, advanced form systems, accessibility, and mobile considerations), condensed version to UIUX-SYSTEM-PROMPT.md (150-200 lines), extend design-tokens.css with form-specific tokens (40-50 new variables), and create 3 progressive HTML/CSS examples demonstrating basic-to-advanced form patterns.

**Tech Stack:** Semantic HTML5, vanilla CSS3 (custom properties, flexbox, transitions), vanilla JavaScript (ES6+), WCAG 2.1 AA accessibility, dark mode via CSS variables.

---

## File Structure

### Files to Create
- `examples/patterns/form-basic-dynamic.html` - Conditional field visibility with simple logic
- `examples/patterns/form-wizard.html` - Multi-step form with progress tracking
- `examples/patterns/form-advanced-dependencies.html` - Complex dependencies, custom validation, field arrays

### Files to Modify
- `core/UIUX-MASTER-PROMPT-SYSTEM.md` - Add Section 30 (950-1100 lines before final checklist)
- `core/UIUX-SYSTEM-PROMPT.md` - Add condensed Section 30 (150-200 lines before checklist)
- `core/design-tokens.css` - Add form builder tokens section (40-50 new variables)

---

## Task 1: Add Section 30 to UIUX-MASTER-PROMPT-SYSTEM.md

**Files:**
- Modify: `core/UIUX-MASTER-PROMPT-SYSTEM.md:7684` (before final checklist)

- [ ] **Step 1: Read the end of MASTER file to understand structure**

Run: `tail -100 core/UIUX-MASTER-PROMPT-SYSTEM.md`

Review the structure around line 7684 (where checklist begins). Note the section format from previous sections.

- [ ] **Step 2: Write comprehensive Section 30 with all subsections**

Insert before the `PRE-GENERATION CHECKLIST` section (before line 7684). The section should cover:
- Section 30 header (title line)
- 30.1 Basic Dynamic Forms (conditional visibility, simple dependencies)
- 30.2 Advanced Form Systems (wizards, complex dependencies, field arrays, validation)
- 30.3 Accessibility patterns (screen readers, keyboard nav, focus management)
- 30.4 Mobile considerations (touch targets, layout, keyboard behavior)

Include complete code examples for:
- Conditional field visibility (checkbox reveals address fields)
- Field dependencies (country → state dropdown population)
- Multi-step wizard (3-4 steps with progress bar, validation, navigation)
- Complex dependencies (user type determines fields)
- Field arrays (add/remove groups)
- Custom validation (patterns, custom rules, error summary)

Total estimated length: 950-1100 lines covering all patterns with inline code blocks and explanations.

- [ ] **Step 3: Run the file to verify it was added correctly**

Run: `tail -150 core/UIUX-MASTER-PROMPT-SYSTEM.md | head -100`

Verify the new Section 30 content is present with proper formatting.

- [ ] **Step 4: Commit Section 30 to MASTER**

```bash
git add core/UIUX-MASTER-PROMPT-SYSTEM.md
git commit -m "feat: add Section 30 - Form Builder Patterns to MASTER

- Basic dynamic forms with conditional field visibility
- Simple field dependencies (country → state)
- Advanced multi-step wizard forms with progress tracking
- Complex field dependencies and conditional validation
- Reusable field groups (add/remove arrays)
- Custom validation and error summaries
- Accessibility patterns for dynamic forms
- Mobile considerations for form UX
- Implementation examples with complete code samples"
```

---

## Task 2: Add Section 30 to UIUX-SYSTEM-PROMPT.md

**Files:**
- Modify: `core/UIUX-SYSTEM-PROMPT.md:619` (before PRE-GENERATION CHECKLIST)

- [ ] **Step 1: Read the current SYSTEM-PROMPT structure**

Run: `tail -50 core/UIUX-SYSTEM-PROMPT.md`

Note the condensed format and style from existing sections.

- [ ] **Step 2: Add condensed Section 30**

Insert before the `PRE-GENERATION CHECKLIST` section (before line 619). Include quick reference patterns for:
- Conditional field visibility (checkbox/select triggers show/hide)
- Field dependencies (dependent selects, option population)
- Multi-step wizards (steps, progress bar, validation, navigation)
- Complex dependencies (configuration-driven field changes)
- Field arrays (add/remove groups)
- Custom validation (patterns, rules, error summaries)
- Accessibility (aria labels, live regions, keyboard nav, focus)
- Mobile (touch targets, layout, viewport behavior)

Total estimated length: 150-200 lines covering key patterns with brief explanations.

- [ ] **Step 3: Verify the condensed section was added correctly**

Run: `tail -60 core/UIUX-SYSTEM-PROMPT.md`

Verify Section 30 appears before the checklist with proper formatting.

- [ ] **Step 4: Commit Section 30 to SYSTEM-PROMPT**

```bash
git add core/UIUX-SYSTEM-PROMPT.md
git commit -m "docs: add condensed Section 30 to SYSTEM-PROMPT

- Quick reference for form builder patterns
- Conditional visibility, dependencies, wizards
- Custom validation and error handling
- Accessibility and mobile considerations"
```

---

## Task 3: Extend design-tokens.css with Form Builder tokens

**Files:**
- Modify: `core/design-tokens.css` (add form tokens section before closing)

- [ ] **Step 1: Read the end of design-tokens.css to understand structure**

Run: `tail -100 core/design-tokens.css`

Note how sections are organized with comments and dark mode adjustments.

- [ ] **Step 2: Add Form Builder tokens section**

Before the last closing brace, add new form builder tokens:

**Form Step/Wizard tokens:**
- `--form-step-indicator-bg`, `--form-step-indicator-text`
- `--form-step-current-bg`, `--form-step-current-text`
- `--form-step-current-shadow`
- `--form-progress-bar-height`, `--form-progress-bar-bg`, `--form-progress-bar-fill`
- `--form-progress-duration`

**Validation state tokens:**
- `--form-error-bg`, `--form-error-border`, `--form-error-text`, `--form-error-icon`
- `--form-warning-bg`, `--form-warning-border`, `--form-warning-text`
- `--form-success-bg`, `--form-success-border`, `--form-success-text`

**Conditional visibility tokens:**
- `--form-transition-duration`, `--form-transition-easing`
- `--form-conditional-opacity-hidden`, `--form-conditional-opacity-visible`

**Dynamic field tokens:**
- `--form-field-add-btn-bg`, `--form-field-add-btn-text`, `--form-field-add-btn-hover-bg`
- `--form-field-remove-btn-bg`, `--form-field-remove-btn-text`, `--form-field-remove-btn-hover-bg`
- `--form-group-border`, `--form-group-spacing`, `--form-group-padding`

**Error summary tokens:**
- `--form-error-summary-bg`, `--form-error-summary-border`, `--form-error-summary-text`

Include dark mode adjustments for all tokens using `@media (prefers-color-scheme: dark)` with appropriate color swaps.

Total estimated: 45-55 new CSS variables with light and dark mode variants.

- [ ] **Step 3: Verify tokens were added correctly**

Run: `grep "form-" core/design-tokens.css | wc -l`

Verify count is 45+.

- [ ] **Step 4: Commit design tokens**

```bash
git add core/design-tokens.css
git commit -m "feat: extend design-tokens.css with form builder tokens

- Form step/wizard indicator tokens
- Validation state tokens (error, warning, success)
- Conditional visibility animation tokens
- Dynamic field add/remove button tokens
- Error summary styling tokens
- Dark mode variants for all form tokens"
```

---

## Task 4: Create form-basic-dynamic.html Example

**Files:**
- Create: `examples/patterns/form-basic-dynamic.html`

**Purpose:** Demonstrate simple conditional field visibility. Shipping address form with checkbox that reveals address fields.

**Key elements:**
- Billing address section (always visible)
- "Ship to different address" checkbox
- Shipping address section (hidden by default, shown when checked)
- Smooth CSS transitions (opacity + max-height)
- Form validation
- Success message on submit
- Accessibility: ARIA labels, live regions, focus management
- Dark mode support
- Mobile responsive

**Structure:**
1. HTML: Form with conditional fieldset (hidden attribute + CSS class)
2. CSS: Transition animations for show/hide, responsive layout, dark mode
3. JavaScript: Handle checkbox change, show/hide fields, move focus, validate form

**Testing checklist:**
- ✅ Checkbox unchecked: fields hidden
- ✅ Checkbox checked: fields appear smoothly with 300ms animation
- ✅ Focus moves to first shipping field when revealed
- ✅ Fields cleared when checkbox unchecked
- ✅ Form validation works (required fields)
- ✅ Success message appears on submit
- ✅ Dark mode colors apply correctly
- ✅ Mobile view (375px): single column, full-width inputs
- ✅ Keyboard navigation (Tab through all elements)
- ✅ Screen reader announces field visibility changes

**Estimated lines: 350-400**

- [ ] **Step 1: Create the file with complete HTML/CSS/JavaScript**

Create `examples/patterns/form-basic-dynamic.html` containing:
- DOCTYPE, meta tags (charset, viewport)
- Embedded style block with design tokens and form styles
- Form HTML with billing + conditional shipping sections
- Embedded script with visibility handling, focus management, form validation
- Complete, standalone file (no external dependencies)

- [ ] **Step 2: Test in browser**

Open file in browser and verify:
- Form displays correctly
- Checkbox interaction works
- Fields fade in/out smoothly
- Focus moves properly
- Form validates and submits
- Success message appears
- Dark mode works
- Mobile layout responsive
- Keyboard accessible

- [ ] **Step 3: Commit the file**

```bash
git add examples/patterns/form-basic-dynamic.html
git commit -m "feat: add form-basic-dynamic.html example

- Demonstrates conditional field visibility
- Shipping address fields hide/show based on checkbox
- Smooth CSS transitions with accessibility support
- WCAG 2.1 AA compliant (proper labels, contrast, focus)
- Mobile responsive (320px - 1920px+)
- Dark mode support
- Full keyboard and screen reader support
- 380 lines of HTML/CSS/JavaScript"
```

---

## Task 5: Create form-wizard.html Example

**Files:**
- Create: `examples/patterns/form-wizard.html`

**Purpose:** Demonstrate multi-step form workflow. 3-step form: Account Info → Profile → Review.

**Key elements:**
- Step indicator (numbered circles, current highlighted)
- Progress bar (updates with each step)
- 3 form sections with different fields
- Previous/Next navigation
- Validation per step (can't proceed with empty fields)
- Final step shows review summary
- Submit button on final step
- Focus management (moves to first input when step changes)
- Screen reader announcements for step changes
- Dark mode support
- Mobile responsive

**Structure:**
1. HTML: Step indicator, progress bar, 3 fieldsets for each step, nav buttons
2. CSS: Step styling, progress bar animation, responsive wizard layout
3. JavaScript: Step navigation, validation, focus management, announcements

**Testing checklist:**
- ✅ All 3 steps display correctly
- ✅ Step indicator shows current step highlighted
- ✅ Progress bar updates (33% → 66% → 100%)
- ✅ Can't proceed without filling required fields
- ✅ Previous button disabled on step 1, enabled on steps 2-3
- ✅ Next button hidden on final step
- ✅ Submit button visible only on final step
- ✅ Focus moves to first input when step changes
- ✅ Screen reader announces step number and label
- ✅ Form submission shows success state
- ✅ Dark mode colors correct
- ✅ Mobile: Steps stack vertically, full-width buttons

**Estimated lines: 500-550**

- [ ] **Step 1: Create the file with complete implementation**

Create `examples/patterns/form-wizard.html` containing:
- Complete HTML with 3 steps (account, profile, review)
- Step indicator bar with CSS styling
- Progress bar with dynamic width
- Form navigation (previous/next)
- Embedded CSS with responsive breakpoints and dark mode
- Embedded JavaScript with step logic, validation, focus management
- Standalone, no external dependencies

- [ ] **Step 2: Test in browser**

Verify:
- All 3 steps render correctly
- Step indicator updates
- Progress bar animates
- Validation prevents advancing without filling fields
- Navigation buttons work
- Focus moves appropriately
- Screen reader announces changes
- Form submission works
- Dark mode and mobile views work

- [ ] **Step 3: Commit the file**

```bash
git add examples/patterns/form-wizard.html
git commit -m "feat: add form-wizard.html example

- Multi-step form (Account → Profile → Review)
- Step indicator with progress bar
- Per-step validation
- Previous/Next navigation
- Keyboard and screen reader support
- Dark mode and mobile responsive
- 525 lines of complete implementation"
```

---

## Task 6: Create form-advanced-dependencies.html Example

**Files:**
- Create: `examples/patterns/form-advanced-dependencies.html`

**Purpose:** Demonstrate complex form logic. User type selection determines which fields appear and their validation rules.

**Key elements:**
- User type select (Individual, Business, Organization)
- Conditional fields based on type:
  - Individual: SSN, Date of Birth
  - Business: Tax ID, Company Size
  - Organization: EIN, Non-profit Status, Board Members (array)
- Field array: Add/remove phone numbers or emails
- Error validation on blur
- Error summary at top with links to invalid fields
- Real-time validation feedback (debounced)
- localStorage persistence (save/restore form state)
- Undo capability
- Dark mode support
- Mobile responsive
- Full accessibility

**Structure:**
1. HTML: User type select, conditional fieldsets, field array markup
2. CSS: Conditional field animations, error summary styling, responsive
3. JavaScript: Field show/hide logic, validation, error summary, localStorage

**Testing checklist:**
- ✅ User type changes visible fields
- ✅ Individual: SSN + DOB appear
- ✅ Business: Tax ID + Company Size appear
- ✅ Organization: EIN + Non-profit + Board members array appear
- ✅ Validation on blur (errors appear)
- ✅ Error summary updates as errors fixed
- ✅ Error summary links move focus to invalid fields
- ✅ Add/remove buttons work on field arrays
- ✅ Remove button disabled when only 1 item
- ✅ localStorage saves form state
- ✅ Page refresh: form data persists
- ✅ Dark mode colors correct
- ✅ Mobile responsive
- ✅ Touch targets 48×48px
- ✅ Keyboard navigation complete

**Estimated lines: 475-525**

- [ ] **Step 1: Create the file with complete implementation**

Create `examples/patterns/form-advanced-dependencies.html` containing:
- User type select with change handler
- Conditional sections for each user type
- Field array implementation (phone numbers or emails)
- Error summary with dynamic list
- Form validation with error message linking
- localStorage for form persistence
- Embedded CSS with all styling
- Embedded JavaScript with complete logic
- Standalone file, no external dependencies

- [ ] **Step 2: Test in browser**

Verify:
- User type changes work correctly
- All conditional fields appear/disappear
- Validation works per field type
- Error summary updates
- Field arrays add/remove
- localStorage persists form
- Page refresh retains data
- Dark mode works
- Mobile layout responsive
- Keyboard and screen reader accessible

- [ ] **Step 3: Commit the file**

```bash
git add examples/patterns/form-advanced-dependencies.html
git commit -m "feat: add form-advanced-dependencies.html example

- Advanced user type selection (Individual/Business/Organization)
- Complex field dependencies and conditional validation
- Error summary with links to fields
- Field arrays (add/remove)
- localStorage persistence
- Real-time validation feedback
- Full accessibility and dark mode
- 500 lines of complete implementation"
```

---

## Task 7: Final Verification and Testing

**Files:**
- All modified and created files

- [ ] **Step 1: Verify all changes present and syntax correct**

Run:
```bash
# Check MASTER has Section 30
grep -c "SECTION 30" core/UIUX-MASTER-PROMPT-SYSTEM.md

# Check SYSTEM-PROMPT has condensed 30
grep -c "SECTION 30" core/UIUX-SYSTEM-PROMPT.md

# Check form tokens in design-tokens.css
grep -c "form-" core/design-tokens.css

# Check all 3 example files exist
ls -lh examples/patterns/form-*.html
```

Expected: 1 Section 30 in MASTER, 1 in SYSTEM-PROMPT, 45+ form variables, 3 HTML files

- [ ] **Step 2: Test all examples in browser**

Open each HTML file in browser and verify:
- Visual layout correct
- Form interactions work (fill, submit, validation)
- Dark mode (browser dark mode setting)
- Mobile view (DevTools, 375px width)
- Keyboard only (Tab, Shift+Tab, Enter/Space)
- Screen reader (test with browser reader mode or screen reader)

Expected: All forms functional, accessible, responsive, no JavaScript errors

- [ ] **Step 3: Verify git status clean**

Run:
```bash
git status
git log --oneline -6
```

Expected: Nothing to commit, 6 commits visible (MASTER, SYSTEM-PROMPT, tokens, basic, wizard, advanced)

- [ ] **Step 4: Final integration check**

Run:
```bash
# Verify no merge conflicts
git diff --name-only

# Check file sizes reasonable
wc -l core/UIUX-MASTER-PROMPT-SYSTEM.md core/UIUX-SYSTEM-PROMPT.md examples/patterns/form-*.html

# Verify Section 30 appears before checklist in MASTER
tail -20 core/UIUX-MASTER-PROMPT-SYSTEM.md
```

Expected: Files have reasonable line counts, MASTER ends with PRE-GENERATION CHECKLIST, no conflicts

- [ ] **Step 5: Final commit if needed**

If all checks pass, no additional commit needed. If issues found:

```bash
git commit -m "fix: [describe fix]"
```

---

## Success Criteria

✅ **Complete:** Section 30 added to MASTER (950-1100 lines)
✅ **Complete:** Condensed Section 30 added to SYSTEM-PROMPT (150-200 lines)
✅ **Complete:** Form builder tokens added to design-tokens.css (45+ variables)
✅ **Complete:** form-basic-dynamic.html created and tested (350-400 lines)
✅ **Complete:** form-wizard.html created and tested (500-550 lines)
✅ **Complete:** form-advanced-dependencies.html created and tested (475-525 lines)
✅ **Complete:** All examples WCAG 2.1 AA compliant
✅ **Complete:** All examples mobile-responsive (320px - 1920px+)
✅ **Complete:** All examples dark mode compatible
✅ **Complete:** All examples keyboard accessible
✅ **Complete:** All examples screen reader tested
✅ **Complete:** 6 focused git commits with clear messages
✅ **Complete:** No conflicts, clean working tree

---

## Summary

This plan implements Section 30 (Form Builder Patterns) for UXPro v2.4.0:

- **Documentation:** 1100+ lines in MASTER teaching AI tools about dynamic forms
- **Quick Reference:** 175+ lines in SYSTEM-PROMPT for copy-paste usage
- **Design Tokens:** 45+ new CSS variables for form styling
- **Working Examples:** 3 progressive examples from basic to advanced
- **Accessibility:** Full WCAG 2.1 AA compliance across all examples
- **Responsive:** All examples work from 320px to 1920px+
- **Git History:** 6 focused, logical commits with clear messages

**Next Steps:** After Section 30 completion, proceed to Section 31 (Advanced Data Tables) and Section 32 (Micro-app Shell) using the same structure and approach.
